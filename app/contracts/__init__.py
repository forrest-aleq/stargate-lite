"""
Locked Provider Contracts for Stargate Lite.

Declares exactly what each consumer (N3, Baby MARS) can call,
what shapes to expect, and what errors mean. Contract tests run
on every commit and fail if the live app drifts from the locked spec.
"""

N3_CONTRACT_VERSION = "1.0.0"
MARS_CONTRACT_VERSION = "1.0.0"
